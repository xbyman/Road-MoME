# 路面多模态协同异常检测 (MoME) 项目报告

## 一、 目前工作总结：从 0 到 1 的突破

在过去的开发周期中，我们成功构建并验证了一个基于专家混合机制 (MoE) 的路面检测系统，处理规模达到了 **10,000 帧级**。

### 1. 高性能数据预处理流水线

- **双模态对齐**：实现了点云 (PCD) 与图像 (JPG) 的时间戳精准对齐，解决了 7,777 帧有效数据的索引问题。
- **深度特征提取**：
   - **2D 纹理**：利用 **DINOv2** 提取了高语义的视觉特征。
   - **3D 几何**：利用 **Point-MAE** 在 4060 显卡上完成了 21 万个 Patch 的几何特征提取。

- **显存优化策略**：通过降低 Batch Size (32 -> 8)、显存主动释放 (empty_cache) 和磁盘定期同步，攻克了 8GB 显存处理大规模 3D Transformer 推理的硬件瓶颈。

### 2. MoME 模型架构与训练

- **多模态专家系统**：构建了包含物理规则专家、3D 几何专家、2D 纹理专家的融合架构。
- **门控决策机制**：模型成功学会了在不同路况下切换“专家”——在明显坑洼处信任几何，在平整路面使用纹理辅助，在复杂路缘切换物理保底。
- **训练表现**：验证集 Loss 稳定在 0.007 左右，曲线收敛极佳，具备了极强的泛化能力。

### 3. 专业级可视化诊断

- **块状热力图**：开发了基于物理坐标还原的 `imshow` 网格渲染脚本，实现了异常概率与专家权重的直观对比，解决了 X 轴显示范围截断的遗留问题。

## 二、 未来工作展望：从 1 到 N 的进阶

目前模型在“感知”层面已经做得很好，未来可以从以下几个维度开展科研或工程优化：

### 1. 时序特征融合 (Temporal Fusion)

- **现状**：目前的检测是“逐帧”进行的，没有考虑前一帧和后一帧的关系。
- **方向**：引入 **GRU** 或 **Temporal Transformer**。当车辆连续经过同一个坑洞时，时序一致性可以大幅降低因光影瞬间跳变导致的误报。

### 2. 弱监督与自监督进化 (Self-Supervised)

- **现状**：目前高度依赖物理计算的伪标签 (Pseudo-labels)。
- **方向**：尝试 **Contrastive Learning (对比学习)**。让模型在没有标签的情况下，学习分辨“正常路面”和“异常区域”的特征分布差异，进一步提升对细微裂缝的敏感度。

### 3. 轻量化与实时化推理 (Deployment)

- **现状**：目前 3D 特征提取速度较慢，无法做到车载实时。
- **方向**：
   - 使用 **TensorRT** 对 DINOv2 和 MLP 进行加速。
   - 探索更轻量级的 3D Backbone (如 **PointNet++** 的精简版) 替代 Point-MAE。

### 4. 极端场景的鲁棒性测试

- **现状**：目前数据主要集中在常规光照下。
- **方向**：收集并测试**夜间、雨天、积水路面**的数据。研究在视觉失效（2D 专家权重降低）时，3D 几何专家是否能完全支撑起检测任务，这对于论文的“消融实验”部分非常有价值。

### 5. 专家稀疏化 (Sparse MoE)

- **现状**：目前三个专家都在同时运行，计算量固定。
- **方向**：引入 **Top-K Gating**。对于非常平整的路面，直接只启动物理专家，完全关闭耗能巨大的 3D/2D 专家，实现计算资源的按需分配。

**结语**：
你现在手握 7,777 帧高质量特征包和训练好的模型，已经拥有了一个非常坚实的“实验平台”。接下来的工作可以更偏向于**算法的精进**或**应用场景的拓宽**。