# 项目初始化与环境配置

## 1. 创建项目目录结构

在终端中运行以下命令，快速构建模块化目录（建议在项目根目录下执行）：

```
mkdir -p Road_Project/{data/{raw_pcd,frame_packages_full},pretrained_models,config,models,scripts,logs}
touch Road_Project/config/config.yaml
touch Road_Project/main.py

```

## 2. Conda 环境安装命令

针对 **Python 3.10** 环境的安装指令如下，已验证对 `Open3D` 和 `PyTorch` 的兼容性：

```
# 1. 创建并激活环境
conda create -n road_env_310 python=3.10 -y
conda activate road_env_310

# 2. 安装 PyTorch (以 CUDA 11.8 为例，4060 显卡建议此版本)
pip install torch torchvision torchaudio --index-url [https://download.pytorch.org/whl/cu118](https://download.pytorch.org/whl/cu118)

# 3. 安装核心依赖库
pip install open3d numpy tqdm pyyaml matplotlib pillow scipy scikit-learn

# 4. 安装模型特定依赖 (Point-MAE 必须)
pip install timm tensorboard

```

## 3. 配置文件与主脚本逻辑

### 3.1 配置文件 (config/config.yaml)

**状态：必须先行配置。**
所有的物理阈值（如 3.5cm）、ROI 范围以及 10k 级数据路径都集中在此管理。修改一处，全局脚本同步生效。

### 3.2 全自动调度器 (main.py)

**状态：最后一步运行。**
在确保 `scripts/` 下的各模块能独立跑通后，通过 `main.py` 的开关控制流水线的起止点，实现端到端的一键训练或推理。

## 4. 4060 显卡特别提醒

由于 4060 拥有 8GB 显存，在运行 `2_extract_3d_deep.py` (3D特征提取) 时，请务必在 `config.yaml` 中将 `inference_batch_size` 设为 **8** 或更小，并配合我们脚本中的 `empty_cache` 逻辑，以防止显存溢出导致死机。